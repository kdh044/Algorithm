#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    long double a , b;
    cin >> a >> b;
     cout << fixed << setprecision(2000) << a/b;
}