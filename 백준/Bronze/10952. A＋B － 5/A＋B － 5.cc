#include <bits/stdc++.h>
using namespace std;

int main(void){
    cin.tie(0);
    ios::sync_with_stdio(0);
    
    while(true){
        int a, b;
        cin >> a >> b;
        if(a == 0 and b == 0) break;
        cout << a + b << "\n";
    }
}